---
- name: Create directories
  hosts: localhost
  become: true

  tasks:
    - name: Create a directory if it does not exist
      ansible.builtin.file:
         path: /home/vagrant/Documents/{{ item }}
         state: directory
         mode: '0755'
      loop:
         - playbooks
         - script-templates
         - text-files
